# BOOTSTRAP.md - 启动引导

## 项目概览

| 项目 | 内容 |
|------|------|
| 项目名 | {{项目名}} |
| 业务描述 | {{业务描述}} |
| 架构模式 | {{架构模式}} |
| 数据底座 | {{数据底座}} |

---

## 就绪检查清单

### 配置就绪
- [ ] `datas/config.json` — 确认项目参数正确
- [ ] `datas/config.schema.json` — 确认配置符合校验规则
- [ ] 存储配置已填写（`config.json.storage.config`）

### 数据底座就绪
- [ ] 数据底座已创建（飞书表格 / 本地文件 / 数据库）
- [ ] 表结构/文件格式已定义
- [ ] 连接信息已配置

### 脚本就绪
- [ ] 业务脚本已编写（`scripts/` 目录）
- [ ] 脚本输入输出格式符合集成契约
- [ ] 脚本可独立运行测试通过

### 定时任务就绪
- [ ] 所有 cron 任务已注册
- [ ] 任务触发动作已验证
- [ ] 失败处理策略已确认

### 架构就绪
- [ ] `ARCHITECTURE.md` 已根据实际业务补充完整
- [ ] 状态机定义与实际逻辑一致
- [ ] 数据流与实际实现一致

---

## 首次启动流程

```
Step 1: 确认配置  →  检查 config.json 和 config.schema.json
Step 2: 连接底座  →  验证数据底座可正常读写
Step 3: 测试脚本  →  逐个运行 scripts/ 下的脚本验证
Step 4: 注册 cron →  注册定时任务
Step 5: 验收指令  →  逐条测试 TOOLS.md 中的指令
Step 6: 正式使用  →  开始录入数据
```

---

## 已知待办

- [ ] {{#待办列表}}
- {{待办}}
{{/待办列表}}
