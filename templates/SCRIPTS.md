# {{项目名}} - 脚本架构

## 脚本类型

| 类型 | 生命周期 | 说明 | 示例 |
|------|---------|------|------|
| **daemon** | 常驻运行 | 守护进程，启动后常驻内存，通过 IPC 通信 | 查询引擎、WebSocket 服务 |
| **cli** | 按需调用 | 命令行客户端，每次调用启动子进程 | 查询工具、数据处理工具 |
| **build** | 一次性运行 | 数据构建/迁移脚本，运行完即退出 | 数据拆分、索引构建 |
| **security** | 一次性运行 | 安全加固脚本，运行完即退出 | 权限设置、命令包装 |
| **one-shot** | 按需调用 | 普通业务脚本，每次调用独立运行 | 数据导出、报告生成 |

---

## 脚本目录结构

```
scripts/
├── daemon/        # 守护进程类（常驻）
├── cli/           # CLI 客户端类
├── build/         # 数据构建类
├── security/      # 安全加固类
└── utils/         # 工具函数类
```

---

## 调用编排

### 编排规则

{{#编排规则}}
### {{场景名}}

```
{{调用流程图}}
```

**并发策略：**
- 可并发执行：{{#可并发}}{{模块}}{{/可并发}}
- 需串行执行：{{#需串行}}{{前置}} → {{后置}}{{/需串行}}

**编排说明：**
{{编排说明}}

{{/编排规则}}

### 通用编排原则

1. **无依赖则并行**：多个独立查询/操作可同时发起
2. **有依赖则串行**：后一步依赖前一步的输出
3. **AI 介入点**：脚本无法完成的部分（如生成内容、智能判断）由 AI 处理
4. **结果合并**：并行结果全部返回后，由 AI 统一组装

### 复杂业务编排（flow/skill 分层）

> 当业务逻辑复杂，AI 直接调用脚本不可靠时，建议采用 flow/skill 分层架构。
> 详细说明见 `ARCHITECTURE.md` 第7节「复杂业务架构参考」。

**核心思路：** AI 只做意图识别 + 参数提取，通过 `run_flow.py` 桥接脚本将确定性逻辑交给 Python 代码执行。

```
AI 层（SOUL.md 路由表）
  ↓ exec run_flow.py <flow_name> '<slots>' '<ctx>'
桥接层（run_flow.py）
  ↓
流程层（flow/）→ 技能层（skill/）→ 公共层（common/）→ 数据层
```

---

## 输出格式规范

### 通用输出格式

所有脚本输出统一为 JSON 格式：

```json
{
  "status": "success | error",
  "data": { ... },
  "error": "错误信息（仅 status=error 时）"
}
```

### 业务输出格式

{{#输出格式}}
#### {{场景}}

```
{{格式模板}}
```

{{/输出格式}}

### 格式原则

1. 同一场景的输出格式**固定不变**
2. 字段顺序固定，不随意调换
3. 空数据用 `{}` 或 `[]`，不用 `null`
4. 错误信息放在 `error` 字段，不混入 `data`

---

## 守护进程管理（仅 daemon 模式）

### 生命周期

```
启动（首次调用自动 fork）
  ↓
常驻运行（Unix Socket / HTTP 监听）
  ↓
状态查询（status）
  ↓
优雅关闭（SIGTERM / stop 命令）
```

### 管理命令

| 操作 | 命令 |
|------|------|
| 手动启动 | `{{守护进程脚本}} daemon` |
| 查看状态 | `{{守护进程脚本}} status` |
| 停止 | `{{守护进程脚本}} stop` |
| 重启 | `{{守护进程脚本}} restart` |

### 通信方式

- **IPC 方式**：{{IPC方式}}
- **超时设置**：默认 {{超时秒数}}s
- **并发限制**：最大 {{最大并发}} 个连接
